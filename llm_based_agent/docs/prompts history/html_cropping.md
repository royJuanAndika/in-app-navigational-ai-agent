
base.instructions.md


this is the design 

09_faq_ingestion_pipeline.md
.


faq_pipeline
 hey dude. i want you to see this. so, there is a problem. the NKG i produced is not reliable. the elements, the triggers, the contains relations are not reliable. so, i need to make sure that the agent fix this. with the intent and HAS_STEP relationships, we can just bypass the dependency to the contains and triggers relations. the intent and the has step will just highlight the element right away. the nkg manual researching with tools will be used only for last resort, if the get intent (that leverages this intent and has step relations) failed. the agent must do the hardwork to actually see the page and generate the steps.

consider this case here

customer_employee.nkg.json
 . for the menambahkan karyawan, the triggers and contains are unreliable. even the element descriptions are not. this is the original html page here

customer_employee.html
 . so, the agent is prone to generating steps that are really step by step. in this add employee form, there are form pages. page 1, page 2, page 3. each containing inputs. to move from page 1 to page 2, the user must click the next button. the step generated by agent tends to skip this. it may skip from step 1 which is an input in page 1, right away to the simpan button that is in page 3. this makes the navigation fail. the page 3 wont be highlighted since it is still hidden.

how do i resolve this dude? how do i make sure of this? should i feed the faq ingestor the actual html right away? i dont think this is possible. should the ingestor read the html pages, but it has tool to search the actual element in the NKG.

think very very very deep. explain me what to do.